# Prototube: typed events in data pipelines

Prototube is a library that enables publishing events with Protobuf schemas. Prototube allows graceful
deployments and evolutions of the data schemas at ease. 

